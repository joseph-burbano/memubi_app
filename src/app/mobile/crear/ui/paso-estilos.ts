/** Tipografía y espacios compartidos por los pasos del asistente. */
export const PASO_ESTILOS = `
  ui-campo { --ui-campo-gap: 0.375rem; --ui-campo-alto: 3.5rem; --ui-campo-padding: var(--ui-space-5); }
  .paso { margin: 0 0 1rem; color: var(--ui-text-secondary); }
  .titulo { margin: 0 0 0.375rem; }
  .ayuda { margin: 0 0 1.25rem; color: var(--ui-text-secondary); }
  .campo-grupo { margin-top: 1.75rem; }
  .rotulo { display: block; margin: 0 0 0.375rem; }
`;
